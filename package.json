{
  "name": "vite-local",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
